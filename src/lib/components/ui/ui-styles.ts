/** Shared layout and typography class strings for product UI. */

export const listRowLinkClass =
	'group -mx-4 block px-4 py-3.5 transition hover:bg-surface-hover/80';

export const nestedListRowClass = 'block px-4 py-3 transition hover:bg-surface-hover/80';

export const tableHeadClass =
	'border-b border-border bg-surface-muted text-xs font-medium uppercase tracking-wide text-ink-muted';

export const tableCellClass = 'px-4 py-3 text-sm';

export const metricValueClass = 'mt-1 font-display text-3xl font-semibold text-ink';

export const metricLabelClass = 'text-sm font-medium text-ink-muted';

/** Wrkspace names, module headers, editable detail titles (e.g. task panel). */
export const pageTitleClass = 'font-display text-2xl font-semibold tracking-tight text-ink';

/** Prominent detail / panel hero titles — large display headings, not form fields. */
export const displayTitleClass =
	'font-display text-3xl font-bold leading-[1.15] tracking-tight text-ink';

export const sectionHeadingClass = 'font-display text-xl font-semibold text-ink';

export const emptyStateClass = 'mt-8 text-center text-sm text-ink-muted';

export const interactiveCardClass =
	'block rounded-xl border border-border bg-surface-raised transition hover:border-accent/40 hover:shadow-sm';

export const iconButtonNeutralClass =
	'inline-flex size-9 items-center justify-center rounded-lg text-ink-muted transition hover:bg-surface-hover hover:text-ink';

export const countPillClass =
	'inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-surface-muted px-1.5 text-[10px] font-medium text-ink-muted tabular-nums';
