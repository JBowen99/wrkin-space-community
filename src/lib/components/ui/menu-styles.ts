/** Shared class strings for menu / dropdown triggers. */

/** Compact ghost trigger for nav, settings, and account menus. */
export const menuTriggerClass =
	'inline-flex items-center gap-1 rounded-md px-2 py-1.5 text-sm font-medium text-ink-muted transition hover:bg-surface-hover hover:text-ink focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/20 disabled:opacity-50';

/** Primary action trigger for create/add menus (matches `Button` primary). */
export const menuTriggerActionClass =
	'inline-flex h-11 items-center gap-1.5 rounded-lg px-4 text-sm font-medium bg-accent text-white shadow-sm transition hover:bg-accent-hover active:scale-[0.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/40 disabled:opacity-50';
